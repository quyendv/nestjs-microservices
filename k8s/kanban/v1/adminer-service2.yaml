apiVersion: v1
kind: Service
metadata:
  name: adminer-service-v2
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  type: NodePort
  selector:
    app: adminer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30200
